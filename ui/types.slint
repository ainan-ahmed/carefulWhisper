// carefulWhisper — Data types shared across the UI

export struct TranscriptItem {
    id: int,
    text: string,
    created-at: string,
    word-count: int,
    duration-s: float,
    backend: string,
}
